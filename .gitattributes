# Do not autoconvert line endings for these test files, they are not supposed
# to be native to your platform.
spec/fixtures/*.md  -text

* -crlf
* text=auto
* text eol=crlf
* text eol=lf
*.properties text eol=crlf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
